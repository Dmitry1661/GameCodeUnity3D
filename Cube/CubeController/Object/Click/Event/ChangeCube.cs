class ChangeCube
{
    /*
    /// <summary>
    /// Сменить куб.
    /// </summary>
    /// <param name="pCubeController"></param>
    public ChangeCube(MBCubeController pCubeController)
    {
        // Если куб не находится в Location.Choice, то нам тут делать нечего.
        if (!new Examination(pCubeController, Location.Choice).Result) return;

        MBUnitController unitController = pCubeController.Maker.UnitController;
        unitController.Event(new ToggleCube());
    }
    */
}
